#include <stdio.h>

/**
 * main - count argc
 * @argc: all parameter
 * @argv: listing parameter
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int count;

	printf("%d\n", argc);
	if (argc > 1)
	{
		for (count = 1; ount < argc; count++)
	{
	}
	}
	else
	{
		return (0);
	}

	return (0);
}
